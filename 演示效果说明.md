# 演示效果说明（Demo Script）

## 1. 演示目标
- 展示两套沉浸式引导模式在地图页的表现差异：任务模式（弹窗强提示）与副驾模式（banner 弱提示）。
- 展示“实时数据变化 → 触发事件 → 局部重规划 → 一键改道”的闭环。
- 展示“截图导入（预约记录/凭证）→ 解析成功 → 写入约束数据”的闭环。
- 展示“进度追踪事件”如何记录用户现场行为。

## 2. 演示环境
- 后端：NestJS 服务运行于 `http://localhost:3000`
- 小程序：地图页（/pages/map/map）已接入后端接口
- 数据：使用 `/parks/dev/seed` 生成 Demo Park 与 POI（4 个点位）

## 3. 演示流程（逐步）

### Step A：打开地图页（沉浸式主舞台）
**画面表现**
- 全屏地图，展示 POI marker
- 底部任务卡显示“未生成路线”
- 右上角显示模式 pill（COPILOT 或 MISSION）

**讲解要点**
- 地图页是唯一会出现强弹窗的页面，避免打断用户在详情页/社区页的浏览。

### Step B：生成路线（Plan Generate）
**操作**
- 点击“生成路线”

**预期效果**
- 底部任务卡更新为下一站 POI（标题 + 步行 ETA + 等待分钟数）
- 地图 marker 可点击查看 callout（包含等待信息）

**讲解要点**
- 路线输出的是“下一步动作”而非单纯列表，降低用户决策负担。

### Step C：模拟排队暴涨触发改道（WAIT_SPIKE）
**操作**
- 在 COPILOT 模式点击“模拟改道”

**预期效果（COPILOT）**
- 顶部出现 banner：提示发现更省时路线与收益（预计省时多少）
- 用户点 banner 后弹出备选列表抽屉/弹层（演示版本用弹窗承载）
- 备选包含 3–4 个选择（演示版本可能只有 1 个）

**讲解要点**
- 副驾模式不抢占，不打断，让用户保持探索感。

### Step D：切换到任务模式（MISSION）并再次触发改道
**操作**
- 点击模式 pill 切换为 MISSION
- 再点击“模拟改道”

**预期效果（MISSION）**
- 直接弹出“路线需要调整”弹窗（仅地图页）
- 只给 2 个决策：暂不调整 / 一键改道
- 弹窗展示一条最优替代项（节省时长、步行距离、等待）

**讲解要点**
- 任务模式适合新手/亲子/省心人群；减少决策次数与分支。

### Step E：一键改道（Apply Alternative）
**操作**
- 点击“一键改道”

**预期效果**
- 任务卡“下一站”替换为备选 POI
- banner/弹窗关闭，地图保持沉浸状态

**讲解要点**
- 改道是“局部重算”，保持计划稳定，避免频繁大范围变化造成焦虑。

### Step F：进度追踪（Progress Events）
**操作**
- 点击“我到了 / 开始排队 / 已完成”

**预期效果**
- 每次操作弹 toast“已记录”
- 后端记录事件：type=arrived/queue_start/done

**讲解要点**
- 进度事件用于后续战报复盘、成就激励、以及对阈值敏感度的个性化调整。

### Step G：导入截图（预约记录/凭证）
**操作**
- 点击“导入截图”，从相册选择官方 App 截图

**预期效果**
- 弹窗展示解析 JSON（page_type + items）
- 若为预约记录：解析出 visit_date、time_window、status、guest_count、guest_name
- 若为预约凭证：解析出日期、时间窗、凭证码（后端会做 hash/last4 处理）

**讲解要点**
- “用户主动上传”是合规路径，可提升路线约束质量（入园窗口/赶场/闭园）。

## 4. 演示中可强调的沉浸细节
- 任务卡只回答“去哪/做什么/为什么现在”，减少信息噪声。
- 改道解释是可解释的：步行更近、等待更少、总耗时更低。
- 强弹窗只在地图页出现，形成明确的“驾驶舱”概念，不在其他页面打断。

## 5. 观众可见的“模式差异总结”
- 任务模式：更主动、更省心、弹窗强提示、备选更少更确定。
- 副驾模式：更自由、更少打扰、banner 提示、备选更多更可选。

